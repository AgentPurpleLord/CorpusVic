pymupdf
rich

# Only needed if you use --backend claude. The default, --backend ollama,
# talks to a local Ollama server over plain HTTP and needs no extra package
# beyond the standard library (see run_pipeline.py --help).
anthropic

// The reading controls on a section page: text size, and whether the
// amendment-history notes are shown. Both are the reader's own
// preference, remembered across pages rather than reset on every link.
